import ReferralsContent from './ReferralsContent';

export const metadata = {
    title: "Referrals | RewaMart",
    alternates: {
        canonical: "https://www.rewamart.co.tz/referrals",
    },
};

export default function ReferralsPage() {
    return <ReferralsContent />;
}
